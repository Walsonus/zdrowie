<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night theme overrides -->
    <style name="Theme.Zdrowie" parent="Base.Theme.Zdrowie">
        <!-- Dark theme specific overrides -->
        <item name="android:windowBackground">@color/background_navy</item>
        <item name="colorSurface">@color/background_navy</item>
        <item name="colorOnSurface">@color/text_light</item>
        <item name="colorSurfaceContainer">@color/primary_blue</item>
        <item name="android:statusBarColor">@color/logo_blue</item>
        <item name="android:navigationBarColor">@color/background_navy</item>
        <item name="android:textColorPrimary">@color/text_light</item>
        <item name="android:textColorSecondary">@color/accent_gold</item>
    </style>
</resources>