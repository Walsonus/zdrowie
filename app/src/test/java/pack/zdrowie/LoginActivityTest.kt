import androidx.test.ext.junit.rules.ActivityScenarioRule
import junit.framework.TestCase.assertNotNull
import org.junit.After
import org.junit.Before
import org.junit.Rule
import org.junit.Test
import org.mockito.kotlin.doReturn
import org.mockito.kotlin.mock
import org.mockito.kotlin.spy
import org.mockito.kotlin.verify
import pack.zdrowie.LoginActivity
import pack.zdrowie.R
import pack.zdrowie.RegisterActivity
import kotlin.jvm.java


class LoginActivityTest {

//    @get:Rule
//    val activityRule = ActivityScenarioRule(LoginActivity::class.java)
//
//    @Before
//    fun setUp() {
//        Intents.init()
//    }
//
//    @After
//    fun tearDown() {
//        Intents.release()
//    }


//    @Test
//    fun emailEditTextIsInitiallyEmpty() {
//        onView(withId(R.id.emailEditText))
//            .check(matches(withText("")))
//        onView(withId(R.id.loginButton)).perform(click())
//
//        // Dodac activity uruvhamiane po logowaniu lub przerobić logowanie ze względu na brak
//        // możliwości użycia w testach prywatnych elementów
//        intended(hasComponent(HomeActivity::class.java.name))
//    }

//    @Test
//    fun `check if email and password fields has values when activity is created`() {
//        onView(withId(R.id.emailEditText)).check(matches(withText("Email")))
//        onView(withId(R.id.passwordEditText)).check(matches(withText("Hasło")))
//    }
}


